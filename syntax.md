```
def bar:m="Bar"
def foo(arg):f=
    "Foo"
    if(arg->f) {
        "bar | | "
    } else {
        "Bar!" 
    }
    
def test:p="Diamonds"
def giveCommand(player, item):f=
    "Player "player "gave "
    if(item->p) {
        "multiple "
    } else {
        "a " 
        if(vowelstart(item)) {
            "n"
        } else {
        }
    }
    item" !"
    
```