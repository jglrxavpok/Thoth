```
def bar:m=Bar
def foo(arg):f=
    Foo
    |arg->f? {|
        bar \| \| |!| Bar! 
    |}|
    
def test:p=Diamonds
def giveCommand(player, item):f=
    Player |player| gave |item->p? {|
        multiple
        |!|
        a|vowelstart(item)? {|n |}|
        |item|
        !
```